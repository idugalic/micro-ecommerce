info:
  build:
    artifact: ${project.artifactId}
    name: ${project.name}
    description: ${project.description}
    version: ${project.version}
    
security:
  user:
    password: password
    
spring:
  cloud:
    config:
      server:
        git:
          uri: https://idugalic@bitbucket.org/idugalic_microservices_lab/micro-ecommerce-config.git

server:
  port: 8888 