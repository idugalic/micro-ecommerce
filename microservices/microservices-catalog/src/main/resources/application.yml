spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
## Info endpoint
info:
  app:
    name: Catalog service
    description: Product Catalog Microservice
  build:
    artifact: ${project.artifactId}
    name: ${project.name}
    description: ${project.description}
    version: ${project.version}
---

spring:
  profiles: default
  jpa:
    database: HSQL

---

spring:
  profiles: cloud
  jpa:
    database: MYSQL
eureka:
  instance:
    hostname: ms-catalog-service.cf.deepsouthcloud.com
    nonSecurePort: 80