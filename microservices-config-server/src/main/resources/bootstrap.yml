spring:
  application:
    name: config-server
  
  cloud:
    config:
      server:
        git:
          uri: https://github.com/idugalic/micro-ecommerce-config.git
    
security:
  user:
    password: password
    
server:
  port: 8888 